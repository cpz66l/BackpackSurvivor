# S19 有来源的按需回忆（2026-09-13）

实现了历史意图路由：`上一趟`、`上次`、`经历`、`战绩`、`记得`、`带回` 等明确询问才进入历史事实路径；普通闲聊和任务概览不再注入历史记录。历史投影最多取上一局快照和三条相关行动记录，物品问询只取有已确认/未带回物品字段的记录，并保留 `recordId` 与来源字段。

UnityMCP 刷新/编译成功，控制台 0 错误；EditMode 53/53 Passed，0 failed，0 skipped；job `94cc854da8d9430996b168b7f30b401b`。此前 S17 请求级审计仍通过，证明历史注入不会破坏会话上下文与隔离。

待人工验收：在营地分别问“最近发生了什么”“上一趟发生了什么”“我上次带回了什么”，确认前者保持近况闲聊、后两者只引用本地记录；退出营地重进后不应记住临时聊天昵称。
