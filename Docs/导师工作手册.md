# 导师工作手册：项目状态快照与教学约定（上下文压缩包）

> 用途：上下文压缩/会话重启后的接续依据。最后更新：2026-07-28（第 14 课收官后）
> 读本文件 + plan.md + Docs/V0.1阶段复盘.md 即可恢复全部工作上下文。

## 一、项目快照

- 工程：`E:\YouXiKaiFa\Backpack Survivor\BackpackSurvivor`，Unity 6000.3.20f1 + URP 17.3.0 + Input System 1.19.0 + Cinemachine
- 脚本根：`Assets/BackpackSurvivor/Scripts/`，命名空间规则 = `BS.` + 目录路径（如 `BS.GamePlay.Combat`、`BS.Core`）
- 场景：仅 `Scenes/Run/01-Run.unity`；Prefabs：Enemy.prefab、Projectile.prefab
- 编码：全部 UTF-8 with BOM（根目录 BackpackSurvivor/.editorconfig 强制）；行尾 CRLF
- 文档：GDD 在仓库根《背包幸存者》游戏设计与实施方案.md；课程文档在 Docs/（第01~13课 + V0.1阶段复盘 + Bug记录簿.md + 性能优化记录.md——两本簿子持续追加，面试素材库）

## 二、进度状态

- ✅ V0.1 战斗核心原型已交付封存（commit dce38dd），超前 GDD 排期约 1 周
- ✅ 第 1~5 课完成并有知识库文档；第 6 课（hygiene 还债）已布置但未做，顺延
- ✅ 第 7 课掉落系统（权重随机+保底）完成：LootTableData/LootRoller/LootManager/DropItem，文档 Docs/第07课-掉落系统与保底机制.md
- ✅ 第 8 课拾取磁吸完成：PickUpMagnet 状态机+加速磁吸、DropItem 静态事件 OnCollected 收货口、PickupLogger 临时听众，文档 Docs/第08课-拾取与磁吸.md
- ✅ 第 9 课背包纯数据网格完成：首个 asmdef（BS.Inventory，No Engine References）、Item/InventoryGrid（四操作+Contains+OnChanged）、六剧本测试全绿；HashSet O(1) 方案经权衡后弃用（48格扫描已够，冗余状态同步成本不值），文档 Docs/第09课-背包纯数据网格.md
- ✅ 第 10 课背包 UI 完成：Canvas 三层（BagPanel/CellLayer/ItemLayer）、InventoryUIController 全量重绘+门闸、拖拽三态+旧锚点回滚+红绿预览、InventorySystem 唯一主人接 OnCollected 自动入包、Rarity 下沉 BS.Inventory、索引器只读窗、PickupLogger 退役；文档 Docs/第10课-背包UI与拖拽.md
- ✅ 第 11 课掉落分层+拾取分化完成：束/叶双形态两级表（DropChannel=probability+subTable 递归）、RollBundle 一次多种、保底只计装备品类；ICollectable 接口+XpOrb+货币池+InventorySystem 双频道；IInteractable（GetPrompt/Interact）+InteractDetector（OverlapSphereNonAlloc 节流+最近目标+event）+InteractPromptUI+E 键；装备摘磁吸落地（trigger 碰撞器+Interactable 层）、经验球 15s/装备加长自然清场；文档 Docs/第11课-掉落分层与交互拾取.md；BUG-002~004 池化三连已归档
- ✅ 第 12 课容器搜刮完成：LootChest 实现 IInteractable（玩家侧零改动）+退役三件套（opened/关碰撞器/变黑留残骸当路标）；宝箱池化+残骸 30s 计时回收+ActiveCount 静态配平；ChestSpawner（EnemyAI.OnEnemyDied 静态事件 K=20 触发+ChestTier 嵌套类等级掷骰+拒绝采样选点 10 次预算+场上上限 5+背压只成功才清零）；飞出散落协程（TrySpawnDrop 改返回 List<GameObject>+DropItem.PlayScatterFlight 抛物线 4t(1-t)+飞行关碰撞器+落地归位）；MapBounds 40m 圆形竞技场单一事实源（GetRandomPoint/IsInside/ClampToInside+Gizmos）+玩家 clamp；BUG-005 双重回收双端设防（Collect 幂等+探测器用完即清）；文档 Docs/第12课-容器搜刮与宝箱系统.md
- ✅ 第 13 课背包交互补丁课完成（原 12.5 课升编）：① BUG-006 提示框全屏透明面板吃射线→CanvasGroup blocksRaycasts=false 系统化解法 ② 丢弃功能（EndDrag 三结局：落放/回滚/面板外丢弃；LootManager 提炼 SpawnEntry 公共生成口；Item→LootEntry 还原往返保真；散落协程复用） ③ R 键旋转（Item Rotated 标志位+base 尺寸+换算属性；TryFindFreeArea 内置双朝向+双失败转回=诚实契约；InputReader OnRotate；BUG-008 OnEnable 早于 Start 的 NRE→引用缓存挪 Awake） ④ 收货口请求-确认（IInteractable.Interact void→bool+CanAccept 侦察兵只问不放+HandleCollected 满包 DiscardToWorld 兜底吐回，吞物品漏洞治本；BUG-007 闪字三连环镜像修复） ⑤ 轻量优化：MapBounds 迁 Scripts/Core/（BS.Core）、Faction.neutral→Neutral 清账；BUG-006~008 已归档，文档 Docs/第13课-背包交互补丁.md
- ✅ 第 14 课合并升级与邻接联动完成：同名同级 2→1 合并升级（CanMerge/TryMerge，拖拽冲突时触发，等级显示）；邻接系统第一版完成“检测 + 表现”（ItemTag / ConnectableSides / AdjacencyRule / AdjacencyEffect / AdjacencyEffectId），双手枪左右相邻触发 DualWield 候选效果；ItemView 接口点灰=可连接、金=已触发；GetActiveSides 用 bitmask 累加，为后续多邻接芯片留接口
- ▶ 当前：阶段 2 第 15 课——背包武器激活系统；默认自动武器激活上限=1，先完成背包武器物品 → 玩家身边自动武器实体的生成/回收与冷却表现，不急着让 DualWield 改战斗
- 课程表（plan.md 已同步，2026-07-28 重排）：6 hygiene（顺延）→ 11 掉落分层 ✅ → 12 容器搜刮 ✅ → 13 背包交互补丁 ✅ → 14 合并+邻接联动 ✅ → 15 背包武器激活 ◀ → 16 邻接结算器+双持兑现 → 17 基础芯片多邻接 → 18 物品/规则配置数据化
- 搜打撤对齐结论：架构零偏移（Loot 管线来源无关），内容配置偏移已记录——普通怪以经验球为主+装备概率掉落已落地（第 11 课）；容器系统已实现（第 12 课）；主动撤离为 GDD 明示的 MVP 暂缓项，不提前做
- 拾取方式分化（2026-07-25 用户提出，GDD 191/201/718 行背书，第 11 课已落地）：货币类磁吸自动拾取；装备类落地无磁吸，近身提示 UI + E 键拾取；宝箱同接口复用（第 12 课）；附近战利品面板仍为可选增强挂账
- 性能课预约（2026-07-26 用户提出）：最小循环跑通后开 Profiler 面板+掉帧排查专题课，已记 plan.md 阶段 3 末尾
- 挂账清单（优先级序）：① 邻接效果结算器（候选→生效；DualWield 每把武器最多参与 1 组，防三持，第 16 课）② 背包武器激活上限与冷却表现（第 15 课）③ 基础芯片多邻接与堆叠/互斥规则（第 17 课）④ 物品标签/接口边/邻接规则数据化，移除临时硬编码（第 18 课）⑤ 旋转态接口方向是否随物品旋转（第 18 课定规矩）⑥ Gold 频道落地（后续接购买生成宝箱）⑦ 提示框世界空间化 ⑧ 附近战利品面板（可选）⑨ 相机 Confiner ⑩ Boss 接线 SpawnChestAt（阶段 4）⑪ 丢弃丢 Rotated 朝向（LootEntry 加字段可解，低优先）⑫ CanAccept 冗余 probe.Rotate() 可删（顺手活）⑬ 连按 E 闪字协程叠加（可缓存句柄 StopCoroutine）

## 三、架构现状（关键类与模式）

| 类 | 位置 | 要点 |
|---|---|---|
| IDamageable / DamageInfo / Faction | Combat | 伤害契约；Faction 有 Player/Enemy/Neutral（第 13 课 neutral→Neutral，hygiene 清账） |
| Health | Combat | 事件 OnDamaged/OnDeath；ResetToFull()；Position=aimPoint/collider中心 |
| HazardZone | Zones | 阵营可配+中立通吃；固定 tick；倒序清理失效目标 |
| EnemyAI | Enemies | 三态 FSM；OnEnable/OnDisable 注册+订阅；Die→pool.Return（null 兜底 SetActive(false)）；实现 IPoolable |
| TargetRegistry | Combat | 静态类花名册；Count 兼作刷怪存活计数（多场景隐患已知，暂缓） |
| WeaponBase(abstract) → AutoWeapon / ActiveWeapon | Combat | AutoWeapon：LateUpdate 转 aimPivot、角度门槛开火；ActiveWeapon：AttackHeld 状态位、首发即时、拍平+判零 |
| Projectile | Combat | SphereCastNonAlloc 扫掠、发射者 root 层级豁免、QueryTriggerInteraction.Ignore、hit.point、IPoolable、Initialize=复位钩子 |
| ObjectPool / IPoolable | Core | Queue+HashSet 防重；预热/弹性扩容（扩容对象首还入籍）；SetPool"车票" |
| EnemySpawner | Waves | 环带（内外半径）+ Deg2Rad；playerTf 可 FindAnyObjectByType 兜底 |
| LootTableData | Data | SO 掉落表束/叶双形态：DropChannel(probability+subTable 递归)/LootEntry(+category/id/amount/width/height)；DropCategory 枚举 Xp/Gold/Equipment |
| LootRoller | Loot | 纯 C# 类：RollBundle 逐频道掷门→子表 Roll；Roll 权重随机+pity 保底（只计装备品类，两条清零路径）；判空守卫齐 |
| LootManager | Loot | 场景入口：TrySpawnDrop(position, bundle) 按品类选池（装备 dropPool/经验 currencyPool 带偏移/Gold 空分支挂账）；dropPool+currencyPool 双池字段；SpawnEntry 公共生成口（第 13 课提炼：宝箱/丢弃/满包兜底共用的单件生成） |
| ICollectable | Loot | 接口 void Collect()；PickUpMagnet 依赖它而非具体类 |
| XpOrb | Loot | IPoolable+ICollectable；15s 自然清场；OnGetFromPool 双归零（pum.StateReset+survivalTimer）；OnCollected 静态事件 |
| DropItem | Loot | IPoolable+ICollectable+IInteractable 三合同；摘磁吸落地旋转；trigger 碰撞器+Interactable 层；GetPrompt 现算；bool Interact=先问 CanAccept 再 Collect（满包 return false 留地上）；PlayScatterFlight 协程（关碰撞器→Lerp+4t(1-t) 抛物线→落地归位→开碰撞器）；Collect 幂等（isCollected 守卫）；OnGetFromPool 归零三项 |
| PickUpMagnet | Loot | 独立磁吸组件：Idle/Attracted 状态机、sqrMagnitude 判距、加速磁吸；StartAttract()；StateReset()；依赖 ICollectable |
| IInteractable | Interaction | 接口 GetPrompt()/bool Interact()（第 13 课 void→bool：请求-确认，失败留地上可重试）；宝箱/撤离终端复用预留 |
| InteractDetector | Interaction | 挂 Player：OverlapSphereNonAlloc 0.1s 节流+buffer[0,count) 纪律+sqrMagnitude 最近+event Action<IInteractable> OnTargetChanged；bool 收货——失败发 event Action OnInteractFailed（目标+提示留着可重试），成功才清双目标+广播 null（治好提示残留） |
| InteractPromptUI | Presentation | 事件驱动木偶：目标 null 隐藏/非 null 先改文本再显示；无 Update 轮询；背包已满闪字协程 0.5s（订阅 OnInteractFailed）；显隐/订阅镜像（BUG-007）；PromptPanel 挂 CanvasGroup blocksRaycasts=false（BUG-006） |
| LootChest | Loot | IPoolable+IInteractable；bool Interact（opened 守卫 return false）；退役三件套（opened/关碰撞器/变黑留残骸）；Initialize(名字/颜色/束) 出池即配置；残骸 30s 计时回收；ActiveCount 静态配平；归零五项 |
| ChestSpawner | Loot | ChestTier 嵌套类（名字/颜色/束/权重）；EnemyAI.OnEnemyDied 计数 K=20 触发；拒绝采样选点（10 次预算+离玩家≥12m）；上限 5；守卫按成本排序；背压只成功才清零 |
| MapBounds | Core（第 13 课已迁，BS.Core） | 40m 圆形竞技场单一事实源：Radius/Center(=自身 transform)/GetRandomPoint/IsInside/ClampToInside（拍平 y+早返回+还 y）；OnDrawGizmos 黄圈 |
| PickupLogger | Loot（临时） | 第 8 课测试订阅者，第 10 课由背包顶替后退役 |
| BS.Inventory.asmdef | Scripts/Inventory | 首个自定义程序集，勾 No Engine References；背包内核物理隔离 UnityEngine |
| Item | Inventory | 纯数据类：Id/Rarity/baseWidth/baseHeight 只读+Rotated 标志（第 13 课）；Width/Height=按 Rotated 换算的只读属性；Rotate() 翻标志 |
| InventoryGrid | Inventory | 6×8 `Item[,]` 存引用非 bool；锚点=左上；CanPlaceAt（判界一行式+重叠扫描）/Place（先 Contains 实例守卫+自调 CanPlaceAt）/Remove（removed 标记，有变化才发 OnChanged）/GetItemAt；TryFindFreeArea 内置双朝向（首朝向找不到→Rotate 再找→双失败转回=诚实契约）；索引器 `this[x,y]` 只读窗；TryGetAnchor；event Action OnChanged |
| InventoryDeBugTest | Tests | 临时 MonoBehaviour 六剧本驱动；以后考虑转 EditMode 测试 |
| Rarity | Inventory | 枚举白绿蓝紫金；第 10 课从 BS.Data 下沉至此（程序集方向规则：共享概念住最底层） |
| InventorySystem | GamePlay | 背包唯一主人：Awake new Grid（全项目唯一）；双频道收货口——DropItem.OnCollected→HandleCollected 入包（满包走 DiscardToWorld 兜底吐回世界，吞物品漏洞治本）/XpOrb.OnCollected→记经验（临时 Log 账本）；CanAccept 侦察兵（复制试放只问不放，请求-确认核心，含冗余 Rotate 挂账）；DiscardToWorld（Item→LootEntry 还原+SpawnEntry 落地+PlayScatterFlight） |
| InventoryUIController | Presentation | 投影渲染器：OnChanged 全量重绘+门闸；拖拽三部曲（拿起即Remove+旧锚点回滚）；EndDrag 三结局（空格落放/冲突回滚旧位或新空位/面板外丢弃）；HandleRotate（R 键：ghost sizeDelta 宽高互换+红绿重算，非拖拽无效）；inputReader 缓存 Awake（BUG-008）；Dragging 用 PointerEventData.position |
| ItemView | Presentation | 木偶+传声筒：Bind(Item) 设尺寸/稀有度色；三指针接口转发控制器；SetValidColor 红绿预览；预制体不能引用场景物体，运行时 FindAnyObjectByType |
| Canvas 结构 | 01-Run | BagPanel(底框) → CellLayer(GridLayout 48格) + ItemLayer(pivot 0,1，兄弟序最后=最上层)；ItemView 预制体在 Prefabs/UI |
| PlayerController / InputReader | Player | InputReader 属性化 {get;private set}；Attack 回调 performed/canceled（持续→布尔位）；Interact 回调→event Action OnInteract（瞬时→事件）；Rotate 回调→event Action OnRotate（第 13 课）；PlayerInput Behavior=Invoke Unity Events；输入资产=Project/Input/GameInput.inputactions（InputSystem_Actions 是模板杂物，勿动） |
| 场景接线 | 01-Run | Player(Tag=Player)：CC+PlayerController+InputReader+Health+AutoWeapon+ActiveWeapon+Gun(aimPivot)+FirePoint+InteractDetector；Pools 物体挂三个 ObjectPool（敌人30/子弹50/经验球）；EnemySpawner、LootManager 挂场景；TagManager 有 Interactable 层 |

## 四、教学约定（导师模式，必须遵守）

1. **用户自己写代码**。导师给：原理讲解、任务拆解、结构提示（伪代码/签名）、验收清单、review。不给完整实现；用户三级求助：概念提示→结构提示→代码片段（只给卡住那段）
2. **每课固定结构**：知识先修 → 任务清单 → 验收清单 → 高频翻车点 → 设计映射/知识库素材
3. **Review 分级**：【必须改】/【建议改】；通过验收才开课下一课；每课结束 git commit
4. **思考题抽查**：课上留的思考题下节课抽答（第 7/8 课思考题已答完并归档进对应文档；仍待抽查：第 5 课"刷怪半径为什么要大于镜头视野"、第 9 课四道已附答案待回问）
5. 代码规范铁律：命名空间=目录；[SerializeField] private + 属性暴露（private 显式写出）；**公开成员 PascalCase / 参数局部 camelCase（第 9 课拧反过一次，盯）**；计时器减法+×deltaTime；Unity 判空用 ==（`?.` 会绕过假 null）；using 人工确认（IDE 乱导入已 9 次事故：第 9 次=InventoryUIController 混进 `using static UnityEditor.Rendering.ShadowCascadeGUI`，commit 前闸首次抓到现行犯；UnityEditor.* 混进运行时代码=打包炸弹）；空 Start/Update 必删；**池化类新增运行期字段→同一分钟 OnGetFromPool 归零（四次复发后的机械流程）**
6. 总结文档格式（Docs/第NN课-*.md，**用户已确认锁定**）：实现了什么(含最终代码)→周期链路图→设计思考→踩坑记录→面试问答→习惯清单→思考题(附参考答案)→下一步
7. 用户学习特征：吸收快、会自主扩展（无敌帧/视野范围/中立区/LateUpdate/NonAlloc/sqrMagnitude/加速磁吸/HandleCollected 满包双保险兜底都是自己加的）；需盯：命名空间裸奔（已犯 3 次）、守卫顺序、改动后忘了 commit、池复用状态归零遗漏

## 五、遗留债务（阶段 2 期间择机清偿）

- PlayerController/InputReader 命名空间+字段封装（第 6 课在处理）
- TargetRegistry 包命名空间（第 6 课）；静态类多场景隐患（场景流课时处理，ActiveCount 同款一并）
- EnemyAI 硬编码找玩家；玩家死亡无流程（待 GameManager）
- 无敌帧从 Health 移除后未回填（数值期评估）
- using 事故计数已 9 次；新增强制闸：commit 前扫改动文件头部 using

## 六、阶段 2 设计要点备忘（GDD 对应节）

- 掉落：权重 白60/绿25/蓝10/紫4/金1；保底=连续 N 次未出蓝以上→强制提稀有，出货清零（GDD 11.4/11.5/20.5）
- 背包：纯数据二维网格先行（6x8），UI 只是投影；占格检测先于拖拽 UI；合并=同名同级 2→1；联动=标签匹配表而非 if-else（GDD 12/20.2/20.4）
- 数据：ScriptableObject 只存静态配置，运行时状态用 Runtime 类不回写（GDD 19.3/19.4/19.5）
